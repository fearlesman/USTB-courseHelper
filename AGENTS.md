# 项目协作说明

## 项目概览

本项目是北京科技大学选课辅助的 Tkinter 桌面程序，入口文件为 `USTB-courseHelper.py`。学年学期计算位于 `academic_term.py`，浏览器启动封装位于 `browser_driver.py`，课程联合查询与字段映射位于 `course_query.py`，用户档案位于 `user_profiles.py`，多用户运行时位于 `multi_user_runtime.py`，命名抢课列表持久化位于 `rush_list_store.py`，这些模块均可在不启动完整业务流程的情况下独立测试。

## 课程查询规则

- 联合查询使用 `p_kcdm_cxrw`（课程代码）和 `p_gjz`（课程名称）；至少需要一个非空条件，避免接口返回过大的响应。
- 课程类型默认“所有”，必须分别尝试查询素质扩展课、专业扩展课、MOOC 和必修课，并按任务 ID 去重汇总全部成功响应；单个类型请求失败不得中断后续类型，只有全部类型失败时才判定整次查询失败。加入抢课列表时使用结果来源的选课方式代码。
- 接口响应必须先通过 `remove_empty_values()` 递归移除 `null`、空文本、空列表和空对象，且不得误删 `0` 与 `false`。
- 展示字段映射集中维护在 `course_query.py` 的 `DISPLAY_COLUMNS` 与 `CourseSearchResult` 中；课程类别仅显示 `kclbmc`，不作为请求筛选参数。
- 会话 Cookie 仅能来自运行时登录结果，严禁写入源码、测试、README、AGENTS.md 或日志。

## 定时抢课规则

- 抢课时间输入框仅在“定时抢课”模式下显示；轮询模式必须隐藏该输入框。
- 定时模式仅负责等待到目标时刻，随后必须复用 `auto_selection_process()` 的持续轮询逻辑，不得维护另一套抢课请求循环。
- 当日目标时刻已到或已过时应立即开始轮询，不得等待到次日。

## 多用户会话规则

- 首次启动且不存在任何用户档案时，独立登录视图必须直接显示居中扫码区域；不得强迫用户先填写别名、创建档案或点击开始登录确认。驱动准备完成后自动创建“默认用户”并启动一次扫码流程，成功后直接进入工作区，不得显示成功或多账号说明弹窗。
- 已有账号再次启动时必须先显示扫码页，ChromeDriver 准备完成后自动为当前账号启动一次登录并生成二维码；已有账号可在自动登录尚未开始时通过“暂不登录，离线进入”打开工作区并抑制待执行的自动登录。仅一个启用账号时不得显示登录目标选择器或“默认用户”内部别名，两个及以上账号时才显示目标选择器。
- 顶栏账号菜单是添加、切换和管理账号的统一入口。添加第二个账号时才要求填写名称；新增账号登录失败时必须恢复添加前的当前账号，新档案保留以便重试，原账号会话、草稿和任务不得改变。
- 快速登录必须复用已有的“默认用户”；若该档案来自旧版本且处于停用状态，应恢复原档案而不是创建同名用户。扫码失败不得自动停用或隐藏默认用户。
- 用户档案由 `UserProfileStore` 管理，持久化内容只有稳定 UUID、唯一别名和时间戳，格式版本为 1；旧版本启用状态字段只作兼容读取，当前界面不得提供停用或恢复入口；不得写入 Cookie、Token、SESSION 或其他凭据。
- 每名用户使用 `user_data/<UUID>/` 保存 `course_list.json` 草稿和 `saved_rush_lists.json` 命名列表。首次创建同名档案时导入旧版按人员命名的两个 JSON 文件，旧文件保留作兼容备份；软删除只修改档案状态，不删除目录和课程。
- `MultiUserRuntime` 为每名用户保存仅在内存中的 `UserRuntimeContext`：Cookie、登录状态、任务状态、课程、定时设置、独立停止事件、线程、查询缓存和日志。程序重启后所有用户恢复为待登录。
- Selenium 扫码登录使用全局串行锁，同一时刻只能为一名用户打开二维码；登录完成后各用户使用自己的 Cookie 和 `requests.Session`，可同时运行最多一个轮询或定时任务。
- 扫码循环必须识别统一认证页面的二维码失效文案，并设置有限兜底超时；失效时必须调用 `fail_login()` 释放全局扫码锁、关闭浏览器并清除旧二维码，使当前用户或其他用户可以重新登录。
- 查询启动时必须捕获发起用户 UUID 与 Cookie，响应只更新原用户查询缓存；切换用户不得串写草稿、命名列表或日志。会话过期和停止操作只作用于目标用户，保活线程按用户分别检查会话。
- 用户切换时渲染空查询缓存不得弹出“未找到符合条件的课程”；只有真实网络查询返回空结果时才允许显示该提示。
- 未登录用户可以离线整理草稿和命名列表；课程查询与抢课启动必须禁用。任务页切换用户时载入该用户模式、定时时间、停止策略、满课策略和日志，运行中的其他用户任务不得被切换操作停止。
- 界面必须通过 `MultiUserRuntime.enabled_contexts()` 和 `active_task_contexts()` 获取启用账号与活动任务快照，不得直接遍历内部 `contexts` 字典。

## 界面规则

- Tkinter 界面使用浅色工作台主题：页面背景为浅灰蓝，信息区域为白色，主操作使用青绿色，停止操作使用红色。
- Notebook 顶层页签固定为“课程查询、抢课任务”，不得增加独立登录标签页。顶栏不得始终显示用户下拉框或“任务：空闲”；未登录时显示扫码入口，任务非空闲时显示任务状态，并始终保留轻量账号菜单。
- 账号菜单在单账号时只显示通用“账号”文本，在多账号时显示当前账号名称和切换项。非当前账号存在等待、抢课或停止中的任务时，顶栏必须显示可点击的任务数量提示。
- 存在启用用户档案时开放两个工作页，但程序启动仍先显示扫码页；登录成功后直接进入抢课任务页。用户选择离线进入或会话失效时仍允许编辑列表，只禁用课程查询和启动抢课。
- 无账号首次视图必须沿用工作区视觉系统：浅灰蓝页面背景和白色 LabelFrame 分区；固定 `400×430` 的扫码主体必须在页面中央醒目显示，二维码放大到约 `300×300` 时使用最近邻插值保持清晰。不得显示左侧说明栏或“开始扫码登录”确认按钮；仅在二维码过期、驱动失败或登录失败后显示“重新生成二维码”。
- 用户管理必须使用从账号菜单打开的独立 `Toplevel` 窗口，不得在登录页嵌入全屏用户档案表；关闭管理窗口不得停止任何账号任务或登录流程。
- 页签切换必须引用 `course_search_tab`、`rush_tab` 等页面对象，禁止通过 `select(1)`、`select(2)` 等数字索引控制业务导航。
- 课程查询页只放置查询条件、加入任务时的优先级、查询操作和结果表；查询结果工具栏必须说明“数字越小越先尝试，相同数字按列表顺序执行”。13 个展示字段必须保留，并提供独立的横向和纵向滚动条。
- 抢课任务页必须始终显示当前用户待抢课程表、课程数量或空状态，并在待抢列表工具栏说明优先级数字越小越先尝试、相同数字按列表顺序执行；人员、模式、策略和任务操作均在该页维护，不得恢复实时控制台区域。真实业务数据必须来自当前用户 `UserRuntimeContext.courses`，旧的全局 `course_data_list` 只能作为无运行时旧测试兼容回退。
- 抢课任务页标题下必须显示当前命名列表名称、备注摘要和未保存状态；保存与管理列表入口位于状态条右侧。
- 任务设置保持人员、模式、停止策略和满课策略四区单行布局；定时时间只在定时模式下显示于第二行。课程添加与删除位于待抢表工具栏，开始与停止位于表格下方右侧。
- 空待抢列表必须提供前往课程查询的操作。窗口最小化、恢复等内部界面事件不得写入业务日志。
- 添加查询结果前必须存在当前抢课人员；校验失败时不得修改课程列表，并应切换到抢课任务页提示用户设置人员。添加成功后必须停留在课程查询页，方便继续查询和添加课程。
- 定时抢课时间输入框仅在定时模式显示；启动任务后必须锁定人员、模式和策略，任务结束后恢复。运行期间还必须禁用查询和添加选中课程操作。
- 抢课运行期间必须同时锁定命名列表的保存、管理、加载、编辑和删除操作。
- 当前列表名称旁必须提供行内编辑入口：已加载列表重命名只改元数据并保留课程脏状态；未命名草稿有课程时创建命名列表；空草稿、非法名称和重复名称必须回滚且不修改当前状态。
- 命名列表管理窗口的“分配给用户”必须调用 `RushListStore.copy_to_users()` 创建深拷贝，目标同名时跳过，不替换目标草稿。
- 默认窗口尺寸为 `1280×860`，最小尺寸为 `1120×720`；两种尺寸下待抢课程表和操作栏不得被裁剪或相互遮挡。
- Treeview 与滚动条统一使用 `grid` 布局；禁止在同一父容器中通过 `pack()` 创建后再调用 `grid_remove()` 隐藏同一表格。

## 文件日志规则

- Tkinter 界面不得创建实时控制台、日志文本框或清空日志按钮；原控制台输出统一交由 `file_logging.py` 写入文件。
- 日志目录固定为项目根目录的 `logs/`，不存在时必须自动创建；文件名采用 `application-YYYY-MM-DD.log`，同日启动追加写入并使用 UTF-8 编码。
- 每条完整日志行必须包含本地时间戳，跨线程写入必须使用锁保护；源码运行允许同时回显原终端。
- `.gitignore` 必须包含 `logs/`，不得提交运行日志文件；Cookie、Token、SESSION 等凭据仍严禁写入日志。

## 命名抢课列表规则

- `user_data/<UUID>/course_list.json` 只表示自动保存的当前草稿，沿用原数组格式且不得自动覆盖命名列表；旧版 `course_list_<人员>.json` 只作为首次导入来源和兼容备份。
- 命名列表只能通过 `rush_list_store.py` 中的 `RushListStore` 访问；界面不得自行解析或写入 `user_data/<UUID>/saved_rush_lists.json`。
- 每位用户使用 UUID 目录中的独立命名列表文件，文档结构为版本 1 的 `{version, lists}`；每条列表必须包含 UUID、名称、备注、创建时间、更新时间和课程快照。
- 名称去除首尾空白后为 1–40 个字符，同一人员下使用 `casefold()` 比较并保持唯一；备注最长 300 个字符；空课程列表禁止保存。
- 课程快照必须深拷贝并移除顶层界面序号 `id`，加载时重新生成从 1 开始的连续序号。
- 命名列表写入必须使用同目录临时文件和 `os.replace()` 原子替换。文件损坏、字段非法或写入失败时必须抛出中文 `RushListStoreError`，界面应保留当前任务不变。
- 加载列表前必须展示名称与课程数量并要求确认；确认后完整替换当前草稿并立即保存。取消或加载失败不得改变当前课程和列表状态。
- 删除当前已加载的命名列表时只删除保存记录，屏幕课程保留，当前状态恢复为未命名且有未保存更改。
- 切换用户时必须保存旧用户界面设置，并加载新用户的草稿、当前命名状态、查询缓存、任务设置和日志；切换不得停止其他用户任务，不同用户不能看到彼此的列表库。

## ChromeDriver 规则

- 登录流程必须通过 Selenium 4.35.0 内置的 Selenium Manager 自动匹配 ChromeDriver，调用 `webdriver.Chrome(options=...)` 时不得传入固定驱动路径。
- 应用初始化时必须以后台线程启动一次 ChromeDriver 预匹配，临时无头浏览器完成缓存后立即关闭；扫码登录必须等待并复用该预匹配结果，不得与预热任务并发重复下载。
- ChromeDriver 预匹配完成前，登录页状态与中央扫码区域必须持续显示“浏览器驱动准备中...”，顶栏登录按钮必须禁用；成功后无需确认并自动启动一次当前账号登录，失败后显示中文失败提示与“重新生成二维码”，点击重试时必须保留并展示原始诊断原因。
- 不得恢复项目内固定版本的 ChromeDriver 或 `webdriver-manager` 依赖；Chrome 更新后的首次登录依赖 Chrome for Testing 下载源，成功后复用用户目录中的 Selenium 缓存。
- 自动匹配失败时，应保留原始异常并提供中文的网络、代理和 Chrome 安装检查提示。

## 学年学期规则

`get_current_academic_term()` 返回接口使用的 `YYYY-YYYY-N` 格式：

- 1 月 1 日至 5 月 31 日：第二学期（`-2`）。
- 6 月 1 日至 7 月 27 日：第三学期（`-3`）。
- 7 月 28 日至 12 月 31 日：第一学期（`-1`）。

7 月 28 日归入第一学期。修改此规则时，必须同步更新 `tests/test_academic_term.py`；若用户可见行为发生变化，还需更新 README 中的学期使用说明。

## 用户文档规则

- `README.md` 面向普通用户，只描述主要功能、安装启动、实际操作流程、数据位置、常见问题和使用限制。
- 接口字段映射、内部存储结构、测试命令、Gherkin 验收场景和开发约束应保留在 `AGENTS.md`、源码或测试中，不得堆叠到 README。
- README 应使用用户可见的界面名称和操作结果，避免使用运行时上下文、接口参数名、内部版本号等实现概念。

## 开发与验证

- 新增 Python 函数必须使用完整类型注解和 Google 风格多行文档字符串。
- 与学年学期有关的修改至少运行：`python -m pytest tests/test_academic_term.py -q`。
- 与浏览器启动有关的修改至少运行：`python -m pytest tests/test_browser_driver.py -q`。
- 与课程联合查询有关的修改至少运行：`python -m pytest tests/test_course_query.py -q`。
- 与定时抢课有关的修改至少运行：`python -m pytest tests/test_rush_schedule.py -q`。
- 与工作区导航或界面布局有关的修改至少运行：`python -m pytest tests/test_ui_workspace.py -q`。
- 与命名抢课列表有关的修改至少运行：`python -m pytest tests/test_rush_list_store.py tests/test_ui_workspace.py -q`。
- 与用户档案或多用户运行时有关的修改至少运行：`python -m pytest tests/test_user_profiles.py tests/test_multi_user_runtime.py tests/test_app_multi_user_flows.py -q`。
- 文档内容使用中文，并在功能完成时同步维护本文件和 `README.md`；README 必须保持面向用户、简洁且以操作流程为主。
